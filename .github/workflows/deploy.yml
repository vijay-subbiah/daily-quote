name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'quote-app/package-lock.json'
        
    - name: Install dependencies
      working-directory: quote-app
      run: |
        # Try npm ci first, fallback to npm install if lock file is out of sync
        npm ci || npm install
      
    - name: Run tests
      working-directory: quote-app
      run: npm run test -- --coverage --watchAll=false
      
    - name: Run linting
      working-directory: quote-app
      run: npm run lint
      
    - name: Build application
      working-directory: quote-app
      run: npm run build -- --configuration production --base-href "/daily-quote/"
      
    - name: Add .nojekyll file to disable Jekyll processing
      working-directory: quote-app
      run: touch dist/quote-app/browser/.nojekyll
      
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './quote-app/dist/quote-app/browser'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
